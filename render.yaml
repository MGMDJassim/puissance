services:
  - type: web
    name: puissance
    env: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/puissance-0.0.1-SNAPSHOT.jar
    autoDeploy: true
